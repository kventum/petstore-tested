GET /findByStatus

- id is filled
- category block is not empty
    - id is filled
- name is filled with a string
- links to one or more photos exist
- status matches to the status model
- filter by query=status (1 status, and 2-3 statuses at once)
- no pets in the response without status filters in query
- response's content type equals application/json